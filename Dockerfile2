From node:latest as build

WORKDIR /backend

COPY . /backend

From postgres

Copy --from=build /backend && npm install -g pm2

RUN cd /backend && npx prisma db push && npm run build

CMD [ "npm", "start","index.js" ]

Expose 8090
