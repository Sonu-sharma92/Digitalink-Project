pipeline {
    agent any

    stages {
        stage('git repo') {
            steps {
                sh 'git branch: 'main', url: 'https://github.com/Sonu-sharma92/Digitalink-Project.git''
            }
        }
    }
}
